{
  "name": "hn-telegram-bot",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that sends daily Hacker News top stories to Telegram",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "trigger": "wrangler dev --test-scheduled",
    "setup": "npm run kv:create && echo 'Now run: npm run secrets'",
    "kv:create": "wrangler kv:namespace create HN_STORIES",
    "secrets": "echo 'Run: wrangler secret put TELEGRAM_BOT_TOKEN' && echo 'Run: wrangler secret put TELEGRAM_CHAT_ID'",
    "test": "wrangler dev --test-scheduled",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck"
  },
  "keywords": [
    "cloudflare-workers",
    "hacker-news",
    "telegram-bot",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260218.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.66.0"
  }
}
